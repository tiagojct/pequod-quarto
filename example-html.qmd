---
title: "Pequod"
subtitle: "A Quarto HTML theme"
author: "Tiago Jacinto"
format:
  pequod-html:
    # Surfaces the navbar toggle that flips data-bs-theme between
    # "light" and "dark". The two stylesheets `pequod-html.scss` and
    # `pequod-html-dark.scss` are compiled together by Quarto and
    # scoped by the attribute.
    theme-toggle: true
---

## Reading first

A pigment-inspired palette rooted in *Moby-Dick*. Warm paper, deep ink,
eight accent hues named after the crew of the *Pequod*.

> Call me Ishmael.

## A little code

```python
def loss(theta, x, y):
    """Mean squared error for a linear model."""
    return ((x @ theta - y) ** 2).mean()
```

Inline code like `loss(theta, x, y)` also picks up the Pequod register.

## Callouts

::: {.callout-note}
Starbuck-blue note rule + tinted header, mirroring reveal and Typst.
:::

::: {.callout-tip}
Tashtego green for tips.
:::

::: {.callout-important}
Ahab red — reserved for the universal stop signal.
:::

## Links

See <https://tiagojct.eu/projects/pequod/> for the full narrative.
